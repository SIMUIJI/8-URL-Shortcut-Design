version: "3.8"

services:
  nginx:
    container_name: nginx
    image: nginx:latest
    ports:
      - 80:80
    build:
      context: .
      dockerfile: dockerfile
    networks:
      - suimb1
    restart: always

networks:
  suimb1:
    external: true